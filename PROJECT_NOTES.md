# 聲調譜 TONESCORE — 台湾華語のリスニング＋発音アプリ

最終更新: 2026-08-19（v10配信。実機報告を順に反映：v10=自然な速さの音節割り当て＋声調の帯、v9=再生音量、v8=末尾の跳ね上がり＋音節ごとの聴き直し、v7=版ずれ事故の対策、v6=相対値での有声判定・1画面UI・ピンイン）
公開URL: https://aratama-ship-it.github.io/tonescore/ （GitHub Pages / main / ルート・公開リポジトリ）
リポジトリ: https://github.com/aratama-ship-it/tonescore
（作業ツリーはiCloud上、gitディレクトリは `~/git-repos/tonescore` に分離 = iCloud破損対策）
★ gh の `--source=.` は gitdir分離だと「not a git repository」で失敗する。
　 `gh repo create <名> --public` → `git remote add origin` → `git push` の順で行う。

このファイルは**別マシンのエージェントが単体で読んで引き継げるよう**自己完結的に書いている。

## 何のためのアプリか

台湾に行く機会が増える本人（元サーカスアーティスト／音楽制作あり）が、
**台湾華語のリスニングと発音（とくに声調）**を鍛えるための自分用PWA。

- 現在のレベル: 簡単な会話はできる。聞き取りと発音の精度を上げたい。
- 使う場面: ①移動中に耳だけで ②現地で使うフレーズの即習得 ③仕事・公演の現場
- 発音の到達点: **声調のピッチ可視化まで**（音声認識による通過判定は後段ウェーブ）

## デザインの核（変えるときはここを読む）

タグライン: **中文を、譜面として読む**

本人は Melodyne でボーカルのピッチカーブを読み慣れている。だから声調を「規則」ではなく
**旋律**として扱い、UIは語学アプリでなく**ボーカルチューナーの譜面**に寄せている。

- 墨のレーンに、**朱＝規範カーブ**と**青磁＝自分の声**の2本だけが立つ。朱は台湾の廟・紅包の色。
- 注音は台湾の教科書どおり**漢字の右に縦組み**（大陸系ピンインアプリと一目で別物になる）。
- 意図的に外したもの: ゲーミフィケーション演出、連続日数バッジ、丸カードの反復、青紫グラデ。
- MVPでも中心の1動作（旋律で掴む → 押しながら発声 → カーブが重なる）は削らない。

## 技術構成

素のHTML/CSS/ESモジュール。ビルド不要。依存パッケージなし。

```
index.html            画面3つ（練習／耳だけ／フレーズ集）
css/app.css
js/app.js             画面の組み立て、canvas描画、音声合成、耳だけモード
js/tones.js           規範カーブ（五度式）、三声変調、旋律の可聴化、声調判定
js/pitch.js           マイク→F0実測（正規化自己相関）、有声区間分割、半音正規化
js/bopomofo.js        ピンイン→注音の決定論的変換（注音を手打ちしないため）
js/data/phrases.js    フレーズ本体（繁体字＋ピンイン＋日本語＋verifyフラグ）
tests/check.mjs       ロジック検証（node tests/check.mjs／45件）
sw.js manifest.json icons/
```

### 設計上の重要な決定（覆す前に読む）

1. **朱の規範カーブは理論値であり、実音声の実測ではない。**
   ブラウザの音声合成の出力波形は解析用に取り出せないため、模範側は五度式
   （1声=55／2声=35／3声=214／4声=51／軽声=短い中低）から描いている。画面にも明記済み。
   実測できるのは利用者自身の声だけ。**「模範音声と実測比較している」と書いてはいけない。**
2. **比較は「形（方向）」のみで、絶対音高は見ない。** 収録ごとの中央F0を0半音として正規化する。
   男女差・体調差・その日の声の高さを吸収するため。判定は tones.js の `judge()`。
3. **時間は音節ごとに正規化する。** 各音節の有声区間を列幅へ引き伸ばして重ねる。
   ここで教えているのは声調の形であって、リズムではない。
4. **三声の変調は「連続する3声のうち最後以外を2声にする」単純規則の近似。**
   3連以上は語の区切りで実際の変調が変わるため、画面に「近似」と明示している。断定しない。
5. **音声合成の声はノベルティ音声を除外し Meijia（美佳／zh-TW）を優先する。**
   macOS/iOSのzh-TWには Eddy・Grandma 等の演出用音声が並び、既定選択だとそれを掴む。
6. **F0のラグ選択は「相関の最大値」ではなく「最初の十分に高い山（最大値の86%以上）」を採る。**
   ラグが伸びると重なる区間が短くなり正規化相関が持ち上がるため、最大値を採ると
   周期の2倍＝1オクターブ下を掴む（147Hz→73.5Hz を実測で確認、2026-08-17に修正）。
   **この判定順を「単純に最大値」へ戻してはいけない。**
7. **相関は12kHz相当まで間引いてから取る。** 48kHzのまま全ラグを走らせると1フレーム約85万回の
   積和になり iPhone の rAF(60fps) に間に合わない。間引き後は 0.068ms/フレーム。
8. **初回のマイク許可ダイアログ中に指が離れる前提で書く。** 押下フラグ(`pressed`)と
   録音中フラグ(`holding`)を分けている。片方だけにすると許可後に録音が止まらなくなる。

### 検証済み / 未検証（2026-08-19時点）

- 検証済み（Claudeがブラウザで実測）:
  - 画面3つの表示、注音の自動変換（不好意思＝ㄅㄨˋ ㄏㄠˇ ㄧˋ ˙ㄙ 等）、變調表示（我也很好→2,2,2,3）
  - 合成フレームを流し込んだ解析→判定→描画の経路（4音節中3音節一致、指摘文の生成）
  - `node tests/check.mjs` 28件すべて通過（合成音でのF0精度80〜400Hz誤差1%以内、雑音の誤検出なし、
    1フレーム0.068ms＝rAF予算16.6msに対して余裕）
  - HTTPS公開後の実測: index/css/js/sw/manifest/icon すべて200、Service Worker がページを制御
  - **オクターブ誤検出を修正済み**（下記）
  - macOS Safari系エンジンに zh-TW 音声 Meijia が存在すること
- **未検証（実機で確かめること）**:
  - ~~iPhone実機のマイク録音とF0抽出~~ → 2回目で取り込み50〜51回/秒・基準103〜113Hzを確認。
    有声率と音節の当たり方は v10 で対策済み、**3回目の確認待ち**
  - ~~録音の聞き返しの音量~~ → v9で解決を本人確認済み（2026-08-19「完璧です」）
  - **自然な速さで音節の割り当てが合うか**（v10の本命。実機未確認）
  - ~~iOS Safari の zh-TW 読み上げ~~ → 実機で動作を本人確認済み（2026-08-18）
  - 耳だけモードの画面ロック中の連続再生（音声合成APIの制約で止まる可能性が高い）
  - ~~PWAとしてのホーム画面追加とオフライン起動~~ → 実機で本人確認済み（2026-08-18）

## フレーズの扱い（ハルシネーション対策）

- `verify: 'ok'` … 台湾で一般に使われる表現として信頼度が高いもの。
- `verify: 'check'` … 意味は通るが台湾での自然さが未確認。**画面に「未確認」と表示される。**
  現在の該当: 「我是雜技演員」（雜技／特技のどちらが自然か）、「燈光可以打亮一點嗎」。
- 注音は**ピンインから自動生成**。ピンイン側が正。データ追加時は `node tests/check.mjs` で
  漢字数とピンイン数の一致が検証される。
- 台湾語彙は意識して選んでいる: 捷運（MRT）／加值（チャージ）／外帶（持ち帰り）／
  不好意思（呼びかけ）／**扯鈴（ディアボロ。大陸の「抖空竹」ではない）**。

## 起動方法

このMacでは `.claude/launch.json` の `tonescore` 設定（port 8947）で開ける。

```bash
node -e "const http=require('http'),fs=require('fs'),p=require('path');const B='/Users/arata/Library/Mobile Documents/com~apple~CloudDocs/claude code files/apps/language-app/tonescore';http.createServer((q,s)=>{let f=decodeURIComponent(q.url.split('?')[0]);if(f==='/')f='/index.html';fs.readFile(p.join(B,f),(e,d)=>{if(e){s.writeHead(404);s.end('404');return}s.writeHead(200,{'Content-Type':{'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json','.png':'image/png'}[p.extname(f)]||'application/octet-stream','Cache-Control':'no-store'});s.end(d)})}).listen(8947,()=>console.log('http://localhost:8947'));"
```

マイクは `localhost` かHTTPSでしか使えない。**iPhone実機で試すにはHTTPS公開が必要。**

## 変更時の作法

- 配信物を変えたら `sw.js` の `VERSION` を上げ、`index.html` の `?v=` も揃えて上げる。
- データやロジックを触ったら `node tests/check.mjs` を通す。
- `window.__tonescore.analyze(frames)` に合成フレームを渡せば、マイクなしで解析経路を確認できる。

## iPhone実機テストの記録（2026-08-18・1回目）

**結果: 声をほぼ拾えなかった（有声率3%・227フレーム／基準ピッチ176Hz）。**
一方で rAF は約57fps 相当で回っており**性能は問題なし**。音声合成（中文を聞く）と
PWA（ホーム画面追加・オフライン起動）は本人確認済みで問題なし。

原因は3つで、いずれも修正済み（v3）。
1. しきい値が机上の値で厳しすぎた → `CLARITY 0.62→0.45` / `RMS_GATE 0.012→0.005` /
   `autoGainControl: true`。**この値は実機実測で決めたもの。机上の値へ戻さない。**
2. 相関の山が雑音でぎざぎざになり「局所最大」条件を満たさないフレームを丸ごと捨てていた
   → 見つからない時は最大値の位置へ落とす（フレームを捨てない方を優先）。
3. `smoothTrack()` を追加（3点メディアン／1フレームの穴埋め／孤立有声の除去）。

**同時に見つかった表示バグ（修正済み）**: 判定欄の診断が初回のまま固定されていた。
`host.after(box)` で新しい要素が先頭に入るのに「最後の1つを残す」処理をしていたため、
新しい方を消していた。実機の3枚が同じ「有声3%・227フレーム」だったのはこれが原因で、
**2枚目以降の実際の数値は不明**（次回の実機テストで取り直す）。

## 取り込み経路の作り直し（v4・2026-08-18）

実機で「押している最中の数値が出ない」との報告から調査。**v3自体は正しく配信されていた**
（live の index.html は v=3、app.js に実況コードあり）ため、端末が古いHTMLを見ていたと判断。
原因と対策：

- **Service Worker がHTMLをキャッシュ優先で返していた** → 更新しても常に1回遅れる。
  HTMLを**ネットワーク優先**に変更（静的物は ?v= 付きなのでキャッシュ優先のまま）。
- **登録スクリプトURLに `?v=` を付けていた** → 更新のたびに別登録になり古いSWが生き残る。
  `register('sw.js')` に固定し、`controllerchange` で一度だけ自動リロード。
- **画面に動作中のバージョンを表示**（ヘッダの `v4`）。実機で「どれが動いているか」を推測しない。

さらに、合成音のマイクストリーム（Oscillator→MediaStreamDestination で getUserMedia を差し替え）で
経路を実走させて2つ見つけた。

- **取り込みを requestAnimationFrame で回していた** → 描画の都合に左右される。タブ非表示で発火せず、
  **iPhoneの低電力モードでは30fpsに落ちてサンプル密度が半分**になる。
  `setInterval(16ms)` へ変更（`SAMPLE_MS`）。音の取り込みに描画の都合を持ち込まない。
- ★**測れていないのに判定を断定していた**。6フレーム／3秒しか無い収録に対して
  「0/8 音節が一致」と表示していた。`analyze()` の冒頭で **0.35秒未満は却下**、
  **25回/秒未満は「間引かれました」と表示して判定を出さない**ようにした。
  診断行にも `256フレーム・63回/秒` を出す。ブラウザで3ケースとも実測確認済み。

## マイク準備の遅さ対策（v5・2026-08-18）

実機から「マイクの許可までやたら時間がかかる」との報告。**体感を推測で語らないため、
所要時間を測って画面に出す**方針にしたうえで、遅さの要因を3つ潰した。

- **初回の準備を明示的な一度きりの手順にした。** `マイクを準備する` ボタンを出し、
  終わったら消える（`rec.ready` で判定）。押した瞬間に待たされる体験を、
  自分で始める手順に置き換える。完了時に `準備できました（0.9秒）` と実測値を出す。
- **押した瞬間にボタンを赤くする**（0.4ms で反応。実測）。準備待ちでも無反応に見えない。
- **MediaRecorder の起動を後回しにした。** 聞き比べ用の録音は「あると嬉しい」機能なのに、
  解析の開始前に起動しており、iOSでは数百ms待たされうる。解析ループを先に回す。
- **`ctx.resume()` を await しない。** 許可ダイアログが出ている間に裏で進ませる。
- **getUserMedia の前に `speechSynthesis.cancel()`。** 読み上げが鳴っているとiOSでは
  オーディオセッションの奪い合いで待たされる。
- 診断行に `準備 ○○ms` を追加（初回のみ）。

ブラウザ実測: 準備ボタン→完了0.9秒（800ms遅延を注入して再現）、2回目以降は
押下から50ms時点で `録音中… 190 Hz ／ 有声 100%`。**待ちは初回だけ**であることを確認済み。
★ただし**許可ダイアログ自体の表示にかかる時間はブラウザ側の都合**で、こちらからは縮められない。
実機の `準備 ○○ms` を見て、どこが遅いかを切り分ける。

## iPhone実機テストの記録（2026-08-18・2回目／v4）

**取り込みは直った：50〜51回/秒。** 基準ピッチ 103〜113Hz（男声として妥当、オクターブ誤検出なし）。
残っていた問題は2つで、v6で対策した。

- **有声率が28〜42%と低い。** 音量の絶対値（RMS_GATE）で切っていたのが原因。
  マイクの入力レベルは端末・口との距離・声量で桁が変わるため絶対値では切れない。
  → `decideVoicing()` を追加し、**その収録の最大音量の9%（約-21dB）**を基準に有声を決める。
  CLARITY も 0.45→0.38。**入力レベルが10分の1でも同じ判定になることをテストで固定**。
- ★**音節の区切りがほぼ常に「均等割」に落ちていた**（実機2枚とも）。
  声が途切れて検出数が音節数と合わないのが実機では普通で、その結果
  **カーブが実際とは違う列に描かれていた**。
  → `segment()` を作り直し、実際の声のかたまりを起点に
  **多ければ間隔の狭い隣同士を結合、足りなければ長い塊を音量の谷で分割**して音節数に合わせる。
  何回結合・分割したかを画面（iシート）に出す。

## UIの作り直し（v6・2026-08-18）

本人の要望：**スクロールなしの1画面に収める／ピンイン表記も欲しい**。

- 練習画面を `100dvh` の flex 列にし、**レーンだけが伸び縮み**する構成へ（`overflow:hidden`）。
  375×667（SE相当）でもスクロールなしで収まることを実測。
- ヘッダを1行に圧縮（ブランド・バージョン・デッキ名・番号・iボタン）。
- 漢字の下に**ピンイン（声調記号つき）**を追加。變調時は實際に読む声調で表示し、注音と同じ橙にする。
- 溢れ対策：見積もりでサイズを置いたあと、`scrollWidth` を見て**収まるまで1pxずつ縮める**。
- **凡例・解説・診断は「i」で開くシート**へ（レーンの上に重ねる）。常時表示すると1画面に入らないが、
  「理論値であって実測ではない」等の但し書きは消してはいけないため、隠さず畳む。
- 判定の詳細は固定要素（`#vdDetail`）に書き込む方式へ。動的挿入をやめ、
  以前の「古い診断が残る」バグの再発余地をなくした。
- 前後の移動は下段の ← → に統合（行数を減らすため）。

## ★配信物の取り違え（v6→v7・2026-08-18）

v6を配信した直後、公開ページが**起動しなくなった**。
`does not provide an export named 'decideVoicing'` ——
**新しい app.js と、キャッシュに残った古い pitch.js** が組み合わさっていた。

`?v=` を付けられるのは HTML から参照する物だけで、**ESモジュールの import 先には
付け忘れが起きる**。付け忘れても壊れない側へ倒した。

- Service Worker を**すべてネットワーク優先**へ（キャッシュはオフライン時の保険だけ）。
- import 先にも `?v=` を付ける（二重の保険）。
- ★**版ずれを機械的に検出するテスト**を追加（`tests/check.mjs` の [0]）。
  `index.html` の `?v=` / `sw.js` の `VERSION` / `app.js` の `APP_VERSION` /
  import先 / `sw.js` の ASSETS を突き合わせる。**以後この事故はテストで止まる。**

版を上げるときは4か所を揃える：`index.html` の `?v=`、`sw.js` の `VERSION` と ASSETS、
`js/app.js` の `APP_VERSION` と import 先。揃っていなければテストが落ちる。

## 末尾の跳ね上がりと、音節ごとの聴き直し（v8・2026-08-18）

実機報告：**「発音の最後がどうやっても上に跳ね上がる」**。
指を離す瞬間の音（タップ音・声の減衰・息）が窓に混ざり、周期推定が短い側へ飛ぶのが原因。
判定は末尾の平均を見るため、**第四声が「落ちていません」に化ける**など結果が壊れる。

- `trimEdges()`：収録の**頭0.04秒・尾0.12秒**を無声にする。尾を厚くするのは害が尾に集中するため。
- `rejectOutliers()`：収録の中央値から**7半音以上**外れたF0を捨てる（一息で1オクターブは動かない）。
- `judge()` を外れ値に強くした。
  - 端の切り落としを**頭5%・尾12%**の非対称に（頭を厚く削ると三声の谷を見失う）。
  - 代表値を平均→**中央値**に。最小・最大も**8%／92%の分位点**に。
    1フレームの飛びで「平らでない」と言わせない。
- 回帰テストを追加（末尾に跳ね上がりを注入して第四声・第一声の判定が動かないこと）。

**音節ごとの聴き直し**（要望：文字ごとにちゃんと対応して録れているか確かめたい）
- レーンの列／漢字／判定マークの**どれをタップしても、その音節だけ再生**する。
  再生中の列は青磁色で光る。
- 解析の時刻（t=0＝押した瞬間）と録音音声の0秒はズレるため、`rec.recOffset`
  （MediaRecorder を起動した時刻）で補正している。
- iシートに**音節ごとの時間範囲と長さ(ms)**を出す。割り当てのずれを目で確認できる。
- ★MediaRecorder の出力は長さ情報を持たないことがあり、その場合シークが効かない。
  効いていなければ黙って頭から流さず「この端末では音節ごとの頭出しができません」と表示する。
  **iPhone実機での頭出しの可否は未確認。**

## 再生音量の問題（v9・2026-08-19）

実機報告：**「一部再生の音量が小さすぎて、iPhoneの音量を最大にしても少ししか聞こえない」**。

原因は「一部再生」固有ではなく**再生全体**の作りにある。
★**iOSはマイクを掴んでいる間、音の出口を受話口（耳に当てる小さいスピーカー）へ切り替える。**
このアプリはマイクを掴みっぱなしにしていた（毎回の準備待ちを避けるため）ので、
録音の聞き返しが常に受話口から小さく鳴っていた。

対策（3つ同時）
1. **再生の前に必ずマイクを手放す**（`rec.release()`）。許可自体は残るので準備ボタンは出ない。
   次に録音するとき `ensure()` が取り直す。
2. **録音中に作った AudioContext を、収録後の最初の再生で作り直す**。
   マイクを手放しても音声セッションを引きずることがあるため（生成は数ミリ秒）。
3. **`<audio>` をやめて Web Audio で鳴らす**。復号して AudioBuffer にし、
   ピークを見て**最大16倍まで持ち上げて**再生する。録音レベルは端末・距離で桁が変わる。
   副産物として、**MediaRecorder の出力で `<audio>` のシークが効かない問題も消えた**
   （AudioBuffer なら任意区間を確実に鳴らせる）。

iシートに `録音のピーク 0.040 → 16.0倍に持ち上げて再生（長さ 3.00秒）` と実測値を出す。
まだ小さければこの数字を見て判断する（ピークが極端に小さいなら録音側、
持ち上げているのに小さいなら出口＝ルーティングの問題）。

## 自然な速さでの音節割り当て（v10・2026-08-19）

実機報告：**「早く喋るとズレる。1音節ずつ喋れば合うが、それでは遅すぎて現実的でない」**。

原因：自然な速さでは音節が繋がり、**無声の切れ目が現れない**。
それまでの「一番音量が小さい所で割る」は、切れ目のない発話に対しては当てずっぽうに近い。

対策：**音量の山（母音）を数えて割る**（`syllableNuclei()`）。
音節はひとつにつき母音＝音量の山を1つ持つ。5点移動平均でならした音量から局所最大を拾い、
**山の際立ち（prominence, dB）**の大きい順に必要数だけ採って、山と山の谷を境界にする。
弱い山（子音の渡り）に釣られない。

- ★**端にある山も数える**（範囲外を -∞ として扱う）。最初と最後の音節の山を
  取りこぼして数が合わなくなる（実装時に実際に踏んだ）。
- 経路の順番：**切れ目の数がぴったり → そのまま／足りない → 山を数える／多すぎる → 結合**。
  無声の切れ目は強い証拠なので、多すぎる場合はそちらを信じる。
- 検証：繋がった発話（無声の切れ目ゼロ・山が5つ）で、境界が山と山の谷にちょうど載ることを実測。

## 声調の帯（v10）

要望：**四声のアクセントをもっと見やすく。グラフの各列の上に対応させて大きく並べたい。**

- グラフの直上に、列と1対1で **その声調の輪郭そのもの**を SVG で大きく描く（`renderToneBar`）。
  記号（ˉˊˇˋ）ではなく形を置くのは、このアプリの言語が「形」だから。
  ここで形を覚えれば下のグラフがそのまま読める。
- 縦は**実際の音域（±6半音）いっぱいに誇張**する。レーンと同じ ±9.5 に合わせると
  形が浅くなって一目で読めない（最初にそれで作って読めなかった）。
- 漢字は音節数で縮むが、**この帯は縮めない**（読みたいのはここだから）。
- 變調の列は橙で、ラベルに `二声←三` と出す。
- レーン下端にあった小さい声調番号は帯と重複するので削除（レーンを広く使う）。

## 用語（v10）

★**「有声率」という専門用語をやめた**（本人に伝わらなかった。2026-08-19）。
`押していた時間のうち、声として拾えたのは 94%（40%を超えていれば十分）` のように、
何の割合で、どのくらいなら十分かまで書く。収録中の表示も `声を拾えています 62%`。

## 録音中の実況をレーンへ（v11・2026-08-19）

実機報告：**「実況の文字が小さすぎるし、押しているボタンの付近なので見えない。
別の場所にポップアップ的に出す必要がある」**。

★**押している指の近くには何も置けない**（手で隠れる）。指から最も遠く、
録音中に空いている**レーンそのもの**を実況に使う。

- 録音中は `state.live` を立て、`drawLive()` がレーンを占有する。
  - 上部に暗い帯を敷き、**Hz を28pxで大きく**、その下に `声を拾えています 87%`。
  - 背後に**話している声のピッチを実時間で1本の線として描く**（青磁）。
    ここでは音節ごとに並べ直さない（並べ直すのは指を離した後）。混同しないよう
    下端に `録音中（実時間 1.4秒）` と出す。
  - 声が来ていなければ朱で `声を拾えていません` ＋ 対処（口を下端に近づける）。
- ★状態は**直近0.25秒の有声**で判断し、無音警告は**0.6秒**声が来ないときだけ出す。
  最後の1フレームで判断すると**音節の切れ目ごとに警告が点滅**して読めない。
- 実況の更新間隔は 55ms（`pitch.js` の報告間隔）。
- 規範カーブは録音中は描かない（時間軸の意味が違うものを重ねない）。

## フレーズの一般語彙拡充（v12・2026-08-19）

本人の要望：「1（フレーズ追加）で。仕事関係に寄せなくていい、一般語彙からやりましょう」。

現地で使う（10）・仕事公演（9）に加え、一般会話デッキを5つ追加（計30フレーズ、全54フレーズ）。

- **挨拶・自己紹介**（6）：名前・出身・言語・学習中
- **移動・交通**（6）：駅・タクシー・バス・高鐵・次の駅
- **買い物・食事**（6）：試着・値引き・持ち帰り・辛さ・返品（既存「現地で使う」の続き）
- **体調・困った時**（6）：腹痛・薬局・迷子・紛失・救急車
- **気持ち・相槌**（6）：驚き・了解・待って・応援

台湾語彙を優先：計程車（大陸は出租車）／公車（大陸は公交車）／高鐵／藥局。
`verify: 'check'`は1件（「這個可以退嗎？」＝返品の言い回しは現地確認が望ましい）。

★注音は全項目 `node tests/check.mjs` で自動変換の未変換ラテン文字なしを確認。
變調（3+3→2+3）も新規フレーズで自動的に効くことをブラウザ実測で確認
（例：「這個可以退嗎？」の 可以＝ke3+yi3 → ㄎㄜˊ に自動変換、聲調帯にも「二声←三」表示）。

## 声調の帯：ラベルをピンインへ（v13・2026-08-19）

本人の要望：「四声ではなくピンインを表示してほしい」。

声調の帯（グラフの各列の直上）のラベルを `四声／三声` のような声調名から、
**変調後の実際のピンイン（声調記号つき）**へ変更した。`toPinyinMarked(withTone(s.py, s.realized))`。
漢字の下に出る `.py` と同じ計算・同じ文字列になる（読む時用と、グラフを見ながら追う時用の
2箇所に同じ情報を置く。冗長ではなく用途が異なる）。

11音節の長いフレーズ（「我要一杯珍珠奶茶，微糖微冰。」）でも折り返し・はみ出しなしを実測済み。

## iPhone実機チェックリスト（3回目。ここから）

公開URLをiPhone Safariで開いて確認する。判定欄に**有声率とフレーム数**が出るので、
うまくいかない時はその数字を見る。

0. **ヘッダ右上のバージョンが `v4` になっているか**を最初に見る。古いままなら一度閉じて開き直す。
1. 押している**最中**にボタン下の表示を見る。`録音中… 186 Hz ／ 有声 62%` のように
   その場で数字が出る。ここが `声を拾えていません` なら発声・距離の問題。
   `取り込みが N回/秒 に間引かれました` なら測定自体が成立していない（判定は出さない）。
2. 離した後の有声率。**40%以上**なら実用域。25%未満なら画面が具体的な指示を出す。
   それでも低ければ `RMS_GATE` をさらに下げる（0.003）。
3. カーブがオクターブ単位で跳ぶなら `TH`（既定 0.86）を上げる。細かく震えるなら
   `smoothTrack()` のメディアン窓を5点に広げる。
4. 「中文を聞く」で台湾の声が出るか。出なければ iOS の
   設定→アクセシビリティ→読み上げコンテンツ→声→中国語 から台湾の声を追加する。
5. ホーム画面に追加 → 機内モードで起動できるか（Service Worker のオフライン確認）。

## 次にやること（優先順）

1. **上のiPhone実機チェック**（F0抽出のしきい値 CLARITY/RMS_GATE/TH の調整はここで初めて判断できる）
2. `verify: 'check'` の表現の現地確認、および公演現場の語彙の拡充（技名・リギング・音響照明）
3. 耳だけモードのロック画面再生（音声ファイル化が必要／TTS音源の生成方法を決める）
4. 音声認識による「通じたか」判定（zh-TW の Web Speech API、iOS実機での挙動確認から）
5. 学習履歴（どの声調で落としやすいかの蓄積）。声調別の弱点が見えると練習が選べる。
